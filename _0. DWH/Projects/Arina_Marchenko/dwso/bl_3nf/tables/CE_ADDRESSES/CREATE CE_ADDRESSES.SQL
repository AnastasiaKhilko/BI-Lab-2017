CREATE TABLE CE_ADDRESSES
  (
    ADDRESSES_ID   NUMBER(8),
    STREET_CODE  VARCHAR2(200),
    HOUSE_NUMBER VARCHAR2(200),
    CITY_ID      NUMBER(8),
    CONSTRAINT ADDRESS_ID PRIMARY KEY (ADDRESSES_ID),
    CONSTRAINT STORE_CITIES FOREIGN KEY (CITIES_ID) REFERENCES CE_CITIES (CITIES_ID)
  );
