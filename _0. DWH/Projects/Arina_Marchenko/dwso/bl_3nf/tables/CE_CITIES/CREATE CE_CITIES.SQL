CREATE TABLE CE_CITIES
  (
    CITY_ID    NUMBER(8),
    CITY_CODE  VARCHAR2(200),
    COUNTRY_ID NUMBER(8),
    CONSTRAINT CITY_ID PRIMARY KEY (CITY_ID),
    CONSTRAINT STORE_COUNTRY FOREIGN KEY (COUNTRY_ID) REFERENCES CE_COUNTRIES (COUNTRY_ID)
  );