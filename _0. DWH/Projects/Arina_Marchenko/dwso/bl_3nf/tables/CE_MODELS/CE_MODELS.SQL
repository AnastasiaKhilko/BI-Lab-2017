CREATE TABLE CE_MODELS
  (
    MODELS_ID   NUMBER(8),
    MODEL_CODE VARCHAR2(200),
    BRANDS_ID   NUMBER(8),
    CONSTRAINT MODELS_ID PRIMARY KEY (MODELS_ID),
    CONSTRAINT MODELS_BRAND FOREIGN KEY (BRANDS_ID) REFERENCES CE_BRANDS (BRANDS_ID)
  );
