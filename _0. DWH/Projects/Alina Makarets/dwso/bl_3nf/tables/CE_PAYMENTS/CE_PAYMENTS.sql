CREATE TABLE CE_PAYMENTS(
        "Payment_srcid" NUMBER(4) NOT NULL,
        "Payment_name" VARCHAR2(40),
        CONSTRAINT "PK_Payment_srcid"
          PRIMARY KEY ("Payment_srcid")
         );
         
